{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // \"\"' | grep -q 'pstack-workspace' && printf '\\n── pstack ──────────────────────────────────────\\nArtifact written. Consider running /critique to score it,\\nor a persona review (/cagan-review, /shreyas-review, etc.)\\nRun /pstack-help for the recommended next step.\\n────────────────────────────────────────────────\\n' || true",
            "description": "Nudge to run /critique after writing a pstack artifact"
          }
        ]
      }
    ]
  }
}
